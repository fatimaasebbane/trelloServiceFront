export const environment = {
  production: true,
  apiUrl: '',

  settings: {
    auth: {
      // keys to store tokens at local storage
      accessTokenKey: '',
      refreshTokenKey: '',
    },
  },
};
